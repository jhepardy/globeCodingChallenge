image: mcr.microsoft.com/playwright:v1.59.1-noble

stages:
  - test

playwright-e2e:
  stage: test
  script:
    - npm ci
    - npx playwright test
  artifacts:
    when: always
    paths:
      - playwright-report/
      - test-results/
    expire_in: 7 days
